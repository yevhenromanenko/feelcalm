{
  "name": "feelcalm-extension",
  "version": "0.1.1",
  "private": true,
  "description": "feelcalm browser extension build pipeline",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:protected": "node scripts/build.mjs --obfuscate",
    "icons:generate": "node scripts/generate-icons.mjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "javascript-obfuscator": "^4.1.1"
  }
}
